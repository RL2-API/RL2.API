diff --git a/RL2-Source/EnemyManager.cs b/Assembly-CSharp/EnemyManager.cs
index 9e22e15..40823ce 100644
--- a/RL2-Source/EnemyManager.cs
+++ b/Assembly-CSharp/EnemyManager.cs
@@ -1,3 +1,5 @@
+using FMOD.Studio;
+using RL2.ModLoader;
 using System;
 using System.Collections;
 using System.Collections.Generic;
@@ -674,6 +676,15 @@ public class EnemyManager : MonoBehaviour
 			{
 				Instance.StartCoroutine(Instance.Internal_RunSummonAnimCoroutine(enemyFromPool, speedMod, disableEnemyOffset: false, hpMod));
 			}
+			foreach (Type globalEnemy in ModLoader.LoadedGlobalEnemies)
+			{
+				GlobalEnemy globalEnemyInstance = enemyFromPool.GameObject.AddComponent(globalEnemy) as GlobalEnemy;
+				if (!globalEnemyInstance.AppliesToEnemyType.Contains(enemyType) || !globalEnemyInstance.AppliesToEnemyRank.Contains(enemyRank))
+				{
+					globalEnemyInstance.enabled = false;
+				}
+				globalEnemyInstance.OnSpawn();
+			}
 		}
 		return enemyFromPool;
 	}
